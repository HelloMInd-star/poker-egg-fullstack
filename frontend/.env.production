# 生产环境变量 - Vercel/Railway 跨域部署时填写
# 部署时请在 Vercel 项目设置中配置以下环境变量（不要把真实值提交到仓库）：
#
#   VITE_API_URL = https://<your-backend>.up.railway.app
#   VITE_WS_URL  = wss://<your-backend>.up.railway.app
#
# 如果前后端同域部署（Nginx/Docker），这两个变量留空即可，使用相对路径通过反代访问API
VITE_ENV=production

# 占位：在部署平台配置真实值。构建时如果为空，代码会使用相对路径。
# VITE_API_URL=
# VITE_WS_URL=
