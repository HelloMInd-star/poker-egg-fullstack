# Poker Egg Fullstack · 仓库审计报告

> 审计日期：2026-08-10
> 仓库：`git@github.com:HelloMInd-star/poker-egg-fullstack.git` (main)
> 本地路径：`/app/data/所有对话/主对话/poker-egg-repo`
> 审计目的：为后续部署（Vercel + Railway）及 AI 逻辑升级（MBTI 人格差异化）做准备。

---

## 一、仓库结构概览

```
poker-egg-repo/
├── README.md                        # 项目说明（含功能、部署、API清单）
├── docker-compose.yml               # 多服务 Docker Compose（dev/prod 两套 profile）
├── .env.example                     # 根级环境变量样例（Postgres 密码、SECRET_KEY）
├── .gitignore / .dockerignore
├── LICENSE
│
├── .github/workflows/deploy.yml     # GitHub Actions：构建前端并发布到 gh-pages
│
├── backend/                         # FastAPI 后端
│   ├── Dockerfile                   # 多阶段构建 (builder / development / production)
│   ├── requirements.txt             # Python 依赖（FastAPI / SQLAlchemy / scikit-learn 等）
│   ├── app.py                       # 主入口（路由 / WebSocket / GameManager）
│   ├── ai/ai_engine.py              # AI 决策引擎（easy/medium/hard 三级）
│   ├── services/game_engine.py      # 游戏引擎（Deck / Card / HandEvaluator / GameEngine）
│   ├── models/
│   │   ├── schemas.py               # Pydantic 请求/响应模型
│   │   └── database.py              # asyncpg 连接 + 内存模式 fallback
│   └── auth/auth.py                 # JWT 签发/解码
│
├── frontend/                        # React 18 + Vite 前端
│   ├── Dockerfile                   # 多阶段构建 (builder / development / production-nginx)
│   ├── package.json
│   ├── vite.config.js               # base 路径 + /api /ws 代理
│   ├── nginx.conf                   # 生产镜像内置 nginx 站点配置
│   ├── .env / .env.production
│   ├── index.html
│   ├── public/easter-egg.html       # 独立彩蛋页面（Y.Mine 主题迷你扑克桌）
│   └── src/
│       ├── main.jsx                 # React 入口
│       ├── App.jsx / App.css        # 根组件（导航、登录/注册 Modal、路由）
│       ├── index.css                # 全局样式
│       ├── store/gameStore.js       # Zustand 全局 store（含原生 WebSocket 逻辑）
│       └── components/
│           ├── PokerTable/          # 牌桌 UI（卡牌、座位、操作按钮）
│           ├── Dashboard/           # 凯利仪表盘（当前为随机模拟数据）
│           ├── GameLobby/           # 大厅（创建/加入房间，列表为 mock）
│           ├── Profile/             # 个人页（用户信息+历史 mock）
│           └── Stats/               # 战绩统计页（recharts 图表，mock 数据）
│
└── nginx/nginx.conf                 # 顶层 Nginx 配置（用于 docker-compose production profile）
```

**关键观察**：
- README 声明存在的 `backend/services/deck_manager.py`、`frontend/src/services/api.js`、`frontend/src/services/socket.js`、`railway.json` 实际**缺失**。
- `frontend/public/easter-egg.html` 是一个**独立可运行**的 HTML（内联 CSS/JS，自带发牌、凯利仪表、表理映射），不依赖 React；它实际包含了项目里最完整的一套"牌桌+凯利"交互，仅在前端通过 `window.open('/easter-egg.html')` 以新窗口打开。
- 后端以**进程内内存字典** (`GameManager.games`) 管理所有游戏会话，未持久化；仅用户注册/统计走 PostgreSQL，PostgreSQL 连不上会降级到内存模式（任意账号可登录）。

---

## 二、技术栈清单

### 前端
| 类别 | 技术 | 版本 | 备注 |
|------|------|------|------|
| UI 框架 | React | ^18.2.0 | 函数组件 + Hooks |
| 构建 | Vite | ^5.0.0 | 支持 base 路径注入 |
| 路由 | react-router-dom | ^6.20.1 | BrowserRouter |
| 组件库 | Ant Design | ^5.11.5 | 含 @ant-design/icons |
| 状态管理 | Zustand | ^4.4.7 | 单一 store `gameStore.js` |
| 动画 | Framer Motion | ^10.16.5 | 卡牌发牌动画 |
| 图表 | Recharts | ^2.8.0 | Stats 页趋势/饼图 |
| HTTP | 原生 fetch / axios(未使用) | ^1.6.2 | 代码里只用 fetch，axios 冗余 |
| 实时通信 | 原生 WebSocket | — | package.json 中 socket.io-client 已安装但**未使用**（冗余依赖） |
| 工具 | dayjs、react-hot-toast | — | hot-toast 代码中未使用（用的是 antd message） |
| 样式 | CSS（BEM 风格，无 CSS-in-JS） | — | 暗紫主题 `#a78bfa` + `#ef4444` |
| Dev | ESLint、Prettier、Jest、@testing-library | — | 无测试文件 |

### 后端
| 类别 | 技术 | 版本 | 备注 |
|------|------|------|------|
| Web 框架 | FastAPI | 0.104.1 | 含 WebSocket 原生支持 |
| ASGI 服务器 | uvicorn[standard] | 0.24.0 | 生产 4 workers |
| 数据库驱动 | asyncpg | 0.29.0 | 异步 PostgreSQL |
| ORM | SQLAlchemy | 2.0.23 | 引入但**实际未使用 ORM**，只直接写 SQL |
| 迁移 | alembic | 1.12.1 | 配置/脚本缺失 |
| 缓存 | redis | 5.0.1 | 引入但**代码中未使用** |
| 异步任务 | celery | 5.3.4 | 引入但**代码中未使用** |
| 认证 | PyJWT + python-jose + passlib[bcrypt] | — | 实际只用 PyJWT；bcrypt **未实际哈希校验密码** |
| AI/ML | scikit-learn、numpy、pandas、joblib | — | 引入但 **AI 决策中完全没用 sklearn**，仅用 random + numpy.random |
| 配置 | pydantic v2、python-dotenv | — | pydantic-settings 未用 |
| 日志 | loguru | — | 代码里用的是标准 logging，loguru 冗余 |
| 测试 | pytest、pytest-asyncio、httpx、pytest-cov | — | 无测试文件 |

### 基础设施
- Docker / docker-compose（多 stage 多 profile）
- GitHub Pages（前端静态托管，目前在线版本就是这里）
- Railway（后端部署，README 提供地址，但仓库缺 `railway.json`）
- PostgreSQL 15 / Redis 7（Compose 中配置）
- Nginx（alpine）

---

## 三、现有功能清单

### ✅ 已实现（代码中真实可运行）
| 功能 | 说明 | 真实度 |
|------|------|------|
| 德州扑克发牌流程 | 洗牌→发手牌→翻牌→转牌→河牌→摊牌 | 基本可用，规则不完整（见问题章节） |
| 牌型评估 | 高牌/对子/两对/三条/顺子/同花/葫芦/四条/同花顺 | 强度值赋值粗糙，比较逻辑不完整 |
| 玩家行动 | 弃牌/过牌/跟注/加注/All-in | 跟注/加注金额计算有 bug |
| 1v1 AI 对战 | 创建游戏即自动加一个 AI Bot | 多 AI 的情况逻辑会出错 |
| AI 三难度 | easy / medium / hard | 规则不同，但整体仍是基于手力 + 随机 |
| WebSocket 实时推送 | 单房间单连接广播 | 广播实现有缺陷（只存最后一个 ws） |
| HTTP API（游戏相关） | create / join / start / action / add-ai / get | 可用 |
| JWT 登录/注册 | 内存模式 + PostgreSQL 模式 | 密码未哈希校验、register 返回结构不规范 |
| 前端牌桌 UI | 5 人圆桌布局、卡牌发牌动画、行动按钮 | 固定 5 个座位的 CSS transform |
| 凯利/表理映射面板（彩蛋页） | `public/easter-egg.html` 中纯前端实现 | 数值为模拟随机数，不基于真实胜率 |
| GitHub Pages 自动部署 | push main 即触发 build & deploy | 工作流正常 |

### ⚠️ 半成品/虚假实现（UI 在、数据假）
| 功能 | 问题 |
|------|------|
| 游戏大厅列表 | `mockGames` 硬编码三行，刷新按钮无实际刷新接口 |
| 加入他人房间 | 后端 GameManager 支持，但前端列表为 mock，无法真加入 |
| 凯利仪表盘（Dashboard.jsx） | 数值全是 `Math.random()`，不接收真实手牌/赔率 |
| 战绩统计（Stats.jsx） | 趋势/饼图/最近对局全是 mock 数据，未调用后端 `/api/stats` |
| 个人页（Profile.jsx） | 用户数据、历史记录均为 mock；设置 tab 空壳 |
| 多人对战 | WebSocket 只保留一个连接对象；座位布局固定 5 个位；无法并发观战 |
| 聊天 | WebSocket 协议定义了 chat，但前端无 UI |
| Docker 生产 profile | `nginx/` 顶层配置 + Dockerfile 可构建，但前后端 base/API URL 未统一 |
| Redis / Celery / sklearn | 依赖装了，代码没用 |

### ❌ README 宣称但实际未实现
- 表理映射面板（React 版本）—— 仅有彩蛋页 HTML 版，React 版 `Dashboard.jsx` 是随机数
- 强化学习 AI（路线图中的）
- 锦标赛模式、移动端 APP
- deck_manager.py、services/api.js、services/socket.js、railway.json（README 结构里有，仓库没有）

---

## 四、AI 对手现状分析

### 4.1 文件位置与类结构
- 代码：`backend/ai/ai_engine.py`
- 入口类：`PokerAI(difficulty)` → `decide_action(game_state)` → 返回 `{"action": fold/check/call/raise/allin, "amount": int}`
- 工厂：`AIDecisionMaker().get_ai(difficulty)` 按 difficulty 缓存实例（但不区分具体玩家，多个同难度 AI 会共享同一实例的 `hand_strength` 字段——**线程/并发隐患**）。

### 4.2 难度差异（本质是参数阈值不同，而非算法不同）

| 参数 | easy | medium | hard |
|------|------|--------|------|
| aggression | 0.1 | 0.3 | 0.5 |
| bluff_frequency | 0.05 | 0.15 | 0.25 |
| thinking_time | 0.5s | 1.0s | 1.5s |
| 决策依据 | 仅看 `hand_strength` 阈值 + 随机 | 加入 pot_odds / EV / 阶段倍率 | 加入 Kelly 公式、诈唬概率、对手 aggression(写死0.3) |

### 4.3 关键逻辑缺陷
1. **手牌评估在 <5 张时不准确**：翻牌前 `all_cards = hole_cards + board_cards` 只有 2 张，直接走 fallback `strength = 0.2 + 0.1 = 0.3`，所有起手牌都被当作相同强度；这意味着 preflop 的 AI 决策基本是**盲的**（全靠阈值/随机）。
2. **没有手牌范围（range）建模**：不区分 AA 和 72o。
3. **没有位置感知**：`position` 参数传入了但从未在决策分支里使用。
4. **没有对手建模**：hard 难度 `opponent_aggression = 0.3` 写死，不根据对手历史调整。
5. **加注金额粗糙**：都是 `int(pot * 系数)`，不考虑最小加注/合法加注规则。
6. **凯利公式名义使用，实未正确**：`_calculate_kelly` 中 `win_rate = min(hand_strength + 0.1, 0.95)` 是粗糙估算，真正凯利应基于胜率/赔率二元投注；`odds = pot/current_bet` 变量名易混（应该是 "b" 或盈亏比）。
7. **没用 sklearn/numpy/pandas**：虽然 requirements 装了一整套 ML 栈，但 AI 完全是规则 + random。
8. **scikit-learn 模型训练接口空**：`AIDecisionMaker.train_from_history()` 是 `pass`。
9. **AI 行动后调用 `process_action` 后用 `last_raise`/`bet` 判断轮次，但游戏引擎的轮次推进规则本身有 bug**（见下节），导致 AI 会错轮/多轮。
10. **多 AI 房间会串行/错乱**：`get_ai_player()` 只返回第一个未弃牌 AI，多 AI 时另一个永远不会被轮询；`process_ai_turn` 只处理一个 AI 后即返回。

### 4.4 MBTI 人格差异化现状
- ❌ **完全没有 MBTI 概念**。
- 当前"人格"只体现在 3 档难度参数（aggression/bluff_frequency），没有：
  - 风险偏好（如 ISTJ 保守 / ENTP 激进）
  - 情绪/tilt 模型
  - 诈唬频率 vs 被加注反应
  - 下注尺度偏好（大下注型/小注纠缠型）
  - 聊天/表情风格（可作为 WebSocket chat 类型消息）
- AI 实例没有"人格 ID/名字/头像"的结构化字段，只有 `name = "AI Bot"` 和一个 difficulty 字符串。

### 4.5 MBTI 接入点建议（详见第八章）

---

## 五、部署配置现状与问题

### 5.1 Docker / docker-compose
**优点**：
- 前后端都有完整的多阶段 Dockerfile（builder / dev / prod），思路专业。
- Compose 使用 profile（`devtools`、`production`）区分场景，Postgres/Redis 配 healthcheck。

**问题**：
1. **生产 profile 下 Nginx 镜像与 Dockerfile 不匹配**：
   - `docker-compose.yml` 的 `nginx` 服务 build 自 `frontend/Dockerfile` target `production`（产物是 `nginx:alpine`，默认配置 `frontend/nginx.conf`）。
   - 但它又挂载了 `./nginx/nginx.conf:/etc/nginx/nginx.conf:ro`（顶层全局配置），**覆盖了镜像内的 `/etc/nginx/nginx.conf`**；而顶层 `nginx/nginx.conf` 的 http 块里是 `server_name localhost; root /usr/share/nginx/html;`，这是对的。
   - 但同时镜像内 `frontend/nginx.conf` 被拷贝到 `/etc/nginx/conf.d/default.conf`，它也定义了 `server { listen 80 }`。两层 server 块可能冲突，**需要验证以实际启动为准**；稳妥做法是二选一。
2. **生产 profile 未设置前端环境变量**：`backend-prod` 服务在 `poker-network` 里 aliases 为 `backend`，`frontend/nginx.conf` 里 `proxy_pass http://backend:5000` 能通过容器 DNS 解析——这块是对的。但 `.env.production` 中所有 `VITE_API_URL`/`VITE_WS_URL` 都被注释，前端在生产镜像里也走相对路径（依赖 Nginx 反代），**在同域名部署场景下是 OK 的**，跨域部署（比如 Vercel+Railway）就必须显式设。
3. **开发模式下的 frontend 容器没有设置 `VITE_API_URL`/`VITE_WS_URL`**，依赖 Vite proxy，这是标准做法，OK。
4. **生产 backend 命令 `--workers 4`** 与内存状态 `game_manager.games` 不兼容：多 worker 下每个 worker 有自己独立的进程内存字典，WebSocket 连接和游戏状态会在 worker 之间不一致。**生产环境必须使用单 worker + 外部共享状态（Redis/DB）**，或者至少在一开始注明。
5. **Compose 默认 `SECRET_KEY=your-secret-key-change-in-production`**，README 有提示，但 `.env.example` 里同样弱密钥。
6. **Postgres 密码默认 `password`**，仅限开发，生产必须替换。
7. **backend 开发容器以 `uvicorn --reload` 启动**，依赖源码挂载，生产没有——OK。

### 5.2 GitHub Actions (`.github/workflows/deploy.yml`)
- 功能：push main → Node 18 → `cd frontend && npm install && VITE_BASE_PATH=/poker-egg-fullstack/ npm run build` → `peaceiris/actions-gh-pages@v3` 发布到 `gh-pages` 分支。
- **问题/注意**：
  1. `VITE_BASE_PATH` 已设置为 `/poker-egg-fullstack/`，对应 GitHub Pages 项目页路径。`vite.config.js` 里 `base: process.env.VITE_BASE_PATH || '/'` 能接收到，正确。
  2. **前端构建产物里 API 请求是相对路径 `/api/*`，而 GitHub Pages 是静态站，没有后端**。所以 Pages 版本要能玩，必须让 `VITE_API_URL`/`VITE_WS_URL` 指向 Railway 后端；但 workflow 里**未设置**这两个变量，意味着部署到 Pages 的版本的 `/api/*` 请求会发到 `hellomind-star.github.io/api/*`，404。
     - **当前在线版本能玩的推测**：可能是后端部署在 Railway + 前端以前单独设过变量，或实际走的是另一份构建；按当前仓库代码，CI 产物默认会走相对路径导致 API 不通。
  3. actions-gh-pages 用的是 `force_orphan: true`，会覆盖 gh-pages 分支历史，是预期行为。
  4. workflow 没有部署后端的步骤（后端 README 说部署在 Railway 但无自动部署配置）。

### 5.3 后端环境变量
后端读取（`database.py` / `auth.py`）：
- `DATABASE_URL`（默认 `postgresql://postgres:password@localhost:5432/poker_egg`）
- `REDIS_URL`（代码里**未读取**！只有 docker-compose 注入了，业务代码没用到）
- `SECRET_KEY`（默认弱密钥）
- 未读但 README 提到：`JWT_ALGORITHM`、`JWT_EXPIRE_MINUTES`、`CORS_ORIGINS`（代码里 CORS 写死 `allow_origins=[..., "*"]`）。

### 5.4 前端环境变量
- `VITE_BASE_PATH`：CI 用，本地/dev 默认 `/`。
- `VITE_API_URL` / `VITE_WS_URL`：`.env` 和 `.env.production` 里都**注释掉了**，前端实际运行时：
  - dev：走 Vite proxy（`/api` → `http://localhost:5000`）。
  - prod（同域 Docker/Nginx）：走相对路径，Nginx 反代。
  - prod（Vercel 单独部署）：**必须显式设置这两个变量**，否则 API/WSS 请求 404。
- `VITE_PROXY_TARGET` / `VITE_WS_PROXY_TARGET`：仅 vite dev server 用，生产无影响。

### 5.5 缺的部署文件
- `railway.json`：README 提到 Railway 部署但仓库未提供。Railway 可以从 Dockerfile/Procfile/nixpacks 自动识别，但自定义启动命令、健康检查、build+start 命令建议显式提供。
- `backend/Dockerfile` 已经能被 Railway 直接 build（Railway root directory 设为 `backend/`），但没有 `.railway/` 或 `railway.json` 说明。
- 没有 `vercel.json`，前端需要 Vercel 时要补充 SPA rewrite 规则。
- 没有 CI 测试、lint 工作流。

---

## 六、需要修复的 Bug / 缺失项（按严重级别）

### 🔴 P0 — 会直接导致运行错误 / 无法启动 / 数据错乱

| # | 文件 | 问题 |
|---|------|------|
| 1 | `backend/auth/auth.py` 末行 | `return self.decode_token(token) is not No` —— **拼写错误**，`No` 未定义，会抛 `NameError`，`verify_token` 不可用。应改为 `None`。 |
| 2 | `backend/auth/auth.py` docstring | 同一位置上方 `"""验证token"""` 被某种编码损坏成乱码 `"""M-iM-*M-^L..."""`（看 hex 是 `e9 aa 8c e8 af 81` = "验证" 的 UTF-8），虽然 Python 能解析，但说明文件曾被错误转码，建议确认。 |
| 3 | `backend/models/database.py` | `authenticate_user` 中"这里简化处理，实际应该使用bcrypt验证"——**密码完全不校验**，输入任意密码只要用户名存在就返回用户；内存模式下甚至任意用户名/密码都能登录。 |
| 4 | `backend/models/database.py` `create_user` | 注册时明文存密码（参数名叫 `password_hash` 但实际拿到的是明文 `user_data.password`，没有任何哈希步骤）。 |
| 5 | `backend/app.py` | WebSocket 连接管理 `self.connections: Dict[str, WebSocket] = {}` **一个 game_id 只存一个 WebSocket**，后连接的人会把前者顶掉；广播只会发给最后一个连接的人，多人在线功能根本不可用。应改为 `Dict[str, List[WebSocket]]`。 |
| 6 | `backend/services/game_engine.py` `process_action` | **raise 逻辑错误**：`raise_amount = min(amount, player.chips)` 直接从玩家 chips 里扣 amount，但没算"需要先 call 再 raise 的差额"，加注实际变成"下注 amount"而不是"跟到当前注再加 amount"。会导致下注金额、底池计算错误。 |
| 7 | `backend/services/game_engine.py` `_get_call_amount` | call 后没有判断本轮是否还有人再加注；`_check_round_complete` 只检查"所有活跃玩家都 has_acted"，没检查"所有人下注额是否相等"。也就是说一旦玩家 A raise 到 80，玩家 B call 到 80，A 已经 acted 但 A 的 bet 现在是 80——这一轮应该结束，但如果 A 之后还可以行动会被遗漏（实际上会错过早结束轮次）。反过来如果 A call 之后 B raise，A 的 `has_acted=True`，本轮也会错误进入下一阶段。 |
| 8 | `backend/services/game_engine.py` `_post_blinds` | 盲注位置固定 `active_players[0]=SB, [1]=BB`，不会旋转（按钮位/庄位永远是第一个玩家），不符合真实扑克规则。 |
| 9 | `backend/services/game_engine.py` `HandEvaluator.evaluate` | 同花判定 `any(suits.count(s) >= 5 for s in set(suits))`：当总牌数 >5（7张：2 手牌+5 公牌）时，同花色 5 张可能**不是连续**的；但由于没筛出"哪 5 张"，顺子+同花的判定会出现**假阳性**——只要有 5 张同花就判同花/同花顺，但这 5 张可能是同花色但不构成顺子，却被同时判 is_straight=true 时会错判同花顺。另外 compare_hands 没比较 kicker，对子/两对/三条等级相同时只比 high_card 不准。 |
| 10 | `backend/services/game_engine.py` | 翻牌前 preflop 行动顺序错误：`current_player_index = 0`，但 BB 位之后应该从 UTG（BB 下一位）开始；当前从小盲开始行动，顺序错。 |
| 11 | `backend/app.py` `process_ai_turn` | 只调用一次 `game.get_ai_player()`（只返回第一个未弃牌 AI），多 AI 不会循环；同时 AI 决策后如果不是最后一个人，应该轮到下一位玩家而非立刻开始下一阶段，轮次切分依赖上面第 7 条有 bug 的 `_check_round_complete`。 |
| 12 | `backend/app.py` lifespan 启动 | `Database.connect()` 失败时 `cls._pool = None`，后续所有 DB 调用都走内存分支，这是"优雅降级"但启动日志仍打 "✅ 数据库连接成功" 会误导——因为 connect 本身 except 后只是 print，不会阻止 lifespan 继续，日志会**错误输出**成功。实际查看 `connect()` 方法，创建 pool 失败后 print 并把 `_pool=None`，但 lifespan 里的 `logger.info("✅ 数据库连接成功")` 在 await 之后**无脑打印**。 |
| 13 | `backend/app.py` | CORS 设置为 `"*"` 且 `allow_credentials=True`，浏览器规范不允许二者共存；带凭证的跨域请求在浏览器中会被拦截。 |
| 14 | `frontend/src/App.jsx` | 使用了 `io from 'socket.io-client'` 但完全没用到（store 里是原生 WebSocket），会让读者误以为用了 socket.io；要么删除依赖要么真接 socket.io。 |

### 🟡 P1 — 功能/体验问题

| # | 文件 | 问题 |
|---|------|------|
| 15 | `frontend/src/components/Dashboard/Dashboard.jsx` | 凯利指数/胜率/张力/DCF 全是 `Math.random()`，不是真实计算，与"凯利公式实时计算"卖点不符。 |
| 16 | `frontend/src/components/GameLobby/GameLobby.jsx` | 房间列表是硬编码 mock 数据；后端也没有 `/api/games`（列出可加入房间）接口。 |
| 17 | `frontend/src/components/Profile/Profile.jsx` / `Stats.jsx` | 同样是硬编码 mock，且 `fetchStats('player1')` 写死 playerId，不是真实登录用户。 |
| 18 | `frontend/src/App.jsx` | 登录后 `setUser(data.user)`，但后端 `/api/auth/login` 返回的是内存模式下 `{"id":"demo_user", "username":..., ...}`，没有 `email` 等字段；Profile 页取 `user.email` 会是 undefined。 |
| 19 | `frontend/src/App.jsx` | 登录/注册 API 路径 `/api/auth/login`，部署到 GitHub Pages / Vercel 子路径时会变成 `https://domain/api/auth/login`——在 Pages 上必然 404。 |
| 20 | `frontend/src/store/gameStore.js` | `VITE_WS_URL` 直接拼接 `wsUrl = ${wsHost}/ws/${gameId}`：如果 `VITE_WS_URL` 已包含路径（例如 `wss://api.xxx.com/ws`）会拼接成 `wss://api.xxx.com/ws/ws/xxx` 双 ws；另外 `wsHost` 没做尾斜杠处理。 |
| 21 | `frontend/src/components/PokerTable/PokerTable.jsx` | 玩家座位用 `index * 72deg` 固定旋转，**硬编码 5 个座位**（360/5=72）。超过或少于 5 个玩家布局会错位/重叠。 |
| 22 | `frontend/src/components/PokerTable/PokerTable.jsx` | `isMyTurn` 判断用 `players[current_player]?.id === playerId`，但 App.jsx 传入的 `playerId={user?.id \|\| 'player1'}`——真实游戏中 player_id 是后端返回的 8 位 UUID 截断，与 user.id 不匹配（登录用户 id 是 `demo_user` 之类），结果**永远不会轮到"你"**，操作按钮始终是灰的。这是当前最致命的前端 bug 之一。 |
| 23 | `backend/app.py` | `/api/game/{game_id}/ai/add` 接口参数 `difficulty: str = "medium"` 是 Query 参数，不是 Body；前端如果 JSON body 传 `{difficulty: 'hard'}` 不会生效。 |
| 24 | `backend/app.py` | `/api/game/action` 检查 `current_player_index` 对应玩家 id 是否等于 action.player_id，和 WebSocket 路径里做了重复校验，但两边都没在 player 行动后把 `current_player_index` 推进到下一位——`GameEngine` 也没有推进 current_player_index 的逻辑！`current_player_index` 初始化为 0 后永不移动，AI 回合是被 `asyncio.create_task(process_ai_turn)` 靠 `get_ai_player()` 抓到，玩家回合永远只等 index=0 的玩家。 |
| 25 | `backend/requirements.txt` | 装了 SQLAlchemy / redis / celery / scikit-learn / pandas / joblib / loguru / alembic 但代码都没用，**镜像体积大、冷启动慢**。 |
| 26 | `frontend/src/App.jsx` | 引入 axios 但只用 fetch，package.json 里的 `react-hot-toast` 也没用到。 |
| 27 | `backend/Dockerfile` production | `CMD ["uvicorn", "app:app", "--workers", "4"]`，多 worker + 内存游戏状态 = 状态分裂；生产单进程 + 单 worker 更安全。 |
| 28 | `backend/models/database.py` | `create_user` 在内存模式下调用了 `import uuid`（文件末尾），但函数里 `str(uuid.uuid4())[:8]` 用的是全局 import，位置靠后没问题，但文件结构很乱（import 放末尾）。 |
| 29 | `frontend/Dockerfile` production | `COPY nginx.conf /etc/nginx/conf.d/default.conf`，与顶层 nginx 挂载的方式并存，部署二选一时容易冲突。 |

### 🟢 P2 — 优化/缺失项
30. 没有单元测试、e2e 测试。
31. 没有 API 限流、没有请求校验（action 金额可以是负数，后端未校验）。
32. 玩家 chips 到 0 不会自动 reload/rebuy，会卡死。
33. 边池（side pot）逻辑完全缺失，all-in 之后其他人再加注计算错误。
34. 没有断线重连（页面刷新就丢局，gameStore 重新连接时游戏没恢复机制）。
35. 日志没有统一格式，用了 `logging` 又装了 loguru。
36. README 里的项目结构图列出不存在的文件（deck_manager.py、services/api.js、railway.json 等）。
37. 没有 CSP/安全头（Nginx 里只加了 X-Frame-Options 和 X-Content-Type-Options，缺 HSTS、Referrer-Policy 等）。
38. 没有 Makefile / Justfile / 一键启动脚本，全靠 README 命令。
39. 彩蛋页 `public/easter-egg.html` 是完全独立的一套实现（纯前端），与后端不连通，仅作为品牌展示；如未来希望 React 版也达到彩蛋页的视觉水平需要对齐。

---

## 七、Vercel 前端部署所需步骤

> 目标：把 `frontend/` 部署到 Vercel，后端部署到 Railway（第八章），通过 `VITE_API_URL` 和 `VITE_WS_URL` 跨域通信。

### 7.1 仓库准备（建议在部署前先修 bug）
至少要先修：
- Bug #1（auth.py `No`→`None`）、#22（playerId 对不上）、#20（WS URL 拼接）、#24（current_player_index 不推进）。

### 7.2 Vercel 配置文件
在 `frontend/` 下新增 `vercel.json`：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

### 7.3 Vite 配置调整
`vite.config.js` 的 `base` 默认 `/`，在 Vercel 上不需要 `/poker-egg-fullstack/` 前缀，保持默认即可。

### 7.4 环境变量（Vercel Project Settings）
```
VITE_API_URL = https://<your-backend>.up.railway.app
VITE_WS_URL  = wss://<your-backend>.up.railway.app
VITE_ENV     = production
```
注意：**不要**在 Vercel 上设 `VITE_BASE_PATH`，默认 `/` 即可。

### 7.5 修改 WebSocket 构造逻辑
`gameStore.js` 里需要修复 bug #20：
```js
const wsBase = import.meta.env.VITE_WS_URL 
  || `${window.location.protocol === 'https:' ? 'wss:' : 'ws:'}//${window.location.host}`;
const wsUrl = wsBase.replace(/\/$/, '') + '/ws/' + gameId;
```
（避免 VITE_WS_URL 带尾斜杠或带 /ws 造成重复路径。）

### 7.6 修改 API base URL
目前所有 fetch 都是相对路径 `/api/...`，跨域部署需要统一加前缀。两种做法：
1. **简单做法**：Vercel 用 rewrites 把 `/api/(.*)` 代理到 Railway 后端：
   ```json
   "rewrites": [
     { "source": "/api/(.*)", "destination": "https://<your-backend>.up.railway.app/api/$1" },
     { "source": "/ws",          "destination": "https://<your-backend>.up.railway.app/ws" }
   ]
   ```
   WebSocket rewrite Vercel 支持有限，建议还是用 VITE_WS_URL 直连。
2. **推荐做法**：封装一个 `apiClient`（用 axios 或 fetch wrapper），读 `VITE_API_URL`；所有调用改成 `\`${API_BASE}/api/...\``。

### 7.7 修复 CORS
后端 `app.py` 需要在 `allow_origins` 里显式加 Vercel 域名：
```python
allow_origins=[
    "http://localhost:5173",
    "https://<your-vercel-app>.vercel.app",
    "https://hellomind-star.github.io",
],
allow_credentials=True,
```
**不要再用 `"*"` + `allow_credentials=True` 组合**，浏览器会拒绝。

### 7.8 Build & Deploy
1. Push 代码到 GitHub。
2. Vercel Dashboard → New Project → Import 仓库 → Root Directory 设为 `frontend/`。
3. Framework 自动识别 Vite；填入环境变量；Deploy。
4. 拿到 `https://xxx.vercel.app`，填到后端 CORS。
5. 如果要保留 GitHub Pages 版本，CI 里 `VITE_BASE_PATH=/poker-egg-fullstack/` + `VITE_API_URL` + `VITE_WS_URL` 一起注入。

### 7.9 彩蛋页
`public/easter-egg.html` 经 Vite 会原样拷贝到 dist 根目录，`/easter-egg.html` 可访问，无需额外配置。

---

## 八、Railway 后端部署所需步骤

### 8.1 前置修复
至少先修：
- Bug #1（auth.py `No`→`None`，否则启动后 JWT 验证路径直接报错）
- Bug #3、#4（密码认证至少用 passlib 做哈希校验）
- Bug #27（生产 workers=4 改为 workers=1，或把状态外置到 Redis）
- Redis/Celery/sklearn 等无用依赖可从 requirements.txt 移除以减镜像体积（可选）。

### 8.2 Railway 项目设置
1. Railway Dashboard → New Project → Deploy from GitHub → 选 `poker-egg-fullstack`。
2. **Root Directory** 设为 `backend/`（Railway 会自动识别 Dockerfile）。
3. 若不用 Dockerfile，也可以用 Nixpacks 自动构建：需要设置
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1`
   （Railway 会注入 `$PORT` 环境变量，默认 uvicorn 要监听 `0.0.0.0:$PORT`；FastAPI 端口写死 5000 会失败！）
   - **所以必须改 Dockerfile/启动命令为从环境变量读端口**：`app.py` 里 uvicorn 也要读 PORT，或把 Dockerfile 的 CMD 改成 `sh -c "uvicorn app:app --host 0.0.0.0 --port ${PORT:-5000} --workers 1"`。

### 8.3 Provision 数据库
在 Railway 项目里：
1. New → Database → **Add PostgreSQL**（会自动注入 `DATABASE_URL` 环境变量）。
2. Redis 目前没用，**可以不添加**（但 `requirements.txt` 里 redis 包不强制连，Dockerfile 也不依赖，启动 OK）。
3. 后端 `database.py` 默认读 `DATABASE_URL` env，拿到 Railway Postgres 的连接串即可直接用（SSL 模式，asyncpg 默认支持，需要可能加 `?ssl=require`——Railway 提供的串通常已包含）。

### 8.4 环境变量
在 Railway Service Variables 中设置：
```
SECRET_KEY          = <随机64位字符串，例如 openssl rand -hex 32>
JWT_ALGORITHM       = HS256
JWT_EXPIRE_MINUTES  = 10080          # 7 天
ENVIRONMENT         = production
DATABASE_URL        = <Railway 自动注入，不要手动覆盖>
PORT                = <Railway 自动注入>
# CORS_ORIGINS（如果实现了白名单读取则设置）
```
删除 Compose 自带的 `REDIS_URL`（不使用 Redis）。

### 8.5 Health Check
Railway → Settings → Healthcheck：
- Path: `/api/health`
- Port: `$PORT`
- Initial Delay: 10s
- Interval: 30s
/health 端点已实现，直接用。

### 8.6 新增 `railway.json`（推荐）
放到仓库根或 `backend/` 下：
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE"
  }
}
```
（若用 Dockerfile 构建，注意 Dockerfile production stage 的 `EXPOSE 5000` 与 Railway `$PORT` 可能不一致，需要让容器内程序真正监听 `$PORT`）。

### 8.7 自定义域名（可选）
Settings → Networking → Custom Domains，绑定自己的域名后填入后端 CORS allow_origins。

### 8.8 WebSocket over WSS
Railway 自动支持 TLS 终结，后端不需要管证书；前端 `VITE_WS_URL` 设为 `wss://<backend>.up.railway.app` 即可。注意 Nginx/反向代理默认会关闭 idle ws，Railway 已经设置好了合适的 timeout，代码里 `pingInterval: 30000`（30s 心跳）是足够的。

### 8.9 数据库迁移
目前代码 `_create_tables()` 是在应用启动时 `CREATE TABLE IF NOT EXISTS`，能满足初始启动；但后续 schema 演进**必须引入 Alembic 迁移**（依赖装了但没配置），否则未来字段变更只能手动 SQL。

---

## 九、MBTI 人格差异化 AI 对手的接入点建议

### 9.1 设计目标
把当前 "easy/medium/hard" 三档一维 AI 升级为 **"难度 × MBTI 人格"** 二维 AI，让 16 型（或 8 型代表）在相同胜率前提下展现可感知的风格差异：
- **下注尺度偏好**（松凶 TAG / 紧凶 LAG / 松弱 / 紧弱 / 岩石型 等）
- **诈唬频率 & 时机**（ENTP/ESTP 等高开放性更高诈唬，ISTJ/ISFJ 几乎不诈唬）
- **情绪/tilt 模型**（连输后 INTJ 更冷静调整，ESFP 可能上头）
- **反应速度方差**（思考型 J 更快决定，P 型随机波动）
- **摊牌倾向**（NF 型容易抓诈唬 call，ST 型偏紧）
- **话术风格**（WebSocket 推送 chat 类型消息，NT 型数据化、NF 型情感化、SJ 型稳健、SP 型挑衅）

### 9.2 数据模型扩展（后端）

1. **新增枚举/配置** `backend/ai/personalities.py`：
```python
MBTI_PERSONALITIES = {
    "INTJ": {"aggression": 0.45, "bluff_freq": 0.15, "call_freq": 0.30,
             "bet_size": "pot",     "tilt_resist": 0.9, "thinking_ms": (900,1500),
             "catch_bluff": 0.30,   "chat_style": "analytical"},
    "ENTP": {"aggression": 0.65, "bluff_freq": 0.35, "call_freq": 0.40,
             "bet_size": "2xpot",  "tilt_resist": 0.6, "thinking_ms": (400,900),
             "catch_bluff": 0.45,  "chat_style": "provocative"},
    "ISTJ": {...},
    # ... 至少覆盖 8 个代表类型：INTJ/ENTP/ISTJ/ESTP/INFP/ESFJ/INTP/ENFJ
}
```
每个 AI 玩家保存 `mbti_type: str`（与 `ai_difficulty` 并存：难度决定"强度上限/底限"，MBTI 决定"风格"）。

2. **Player 数据类扩展**（`services/game_engine.py`）：
```python
@dataclass
class Player:
    ...
    mbti_type: Optional[str] = None   # 新增
    stats: Dict = field(default_factory=lambda: {
        "vpip": 0.0, "pfr": 0.0, "aggro": 0.0,
        "hands_played": 0, "tilt": 0.0, "c_deals": 0,
    })
```
新增 `mbti_type` 字段，在 `add_player(is_ai=True, ...)` 时可传入。

3. **新建接口**：
   - `POST /api/game/{id}/ai/add` 增加 body 参数 `mbti_type?: string`
   - 新增 `POST /api/game/{id}/ai/random`：随机选一个 MBTI 作为 AI 人格（可在大厅作为"随机 AI 对手"按钮）
   - WebSocket 推送 `ai_persona` 消息：告诉前端 AI 名字/头像/MBTI 标签/聊天风格。

### 9.3 AI 决策层改造（`backend/ai/ai_engine.py`）
将当前 `PokerAI._easy_decision/_medium/_hard_decision` 三个分支重构成：

1. **基类 `BasePokerAI`**：持有 `personality` 配置、难度参数；提供以下钩子方法：
   - `evaluate_hand_strength(hole, board) -> float`（独立抽离，preflop 用起手牌胜率查表代替当前 0.3 常量）
   - `estimate_opponent_range(history) -> Range`（预留接口）
   - `select_action(hand_strength, pot_odds, ev, position, ...) -> Decision`
   - `choose_bet_size(pot, hand_strength, intent) -> int`（不同 MBTI 不同倍率）
   - `should_bluff(board, history, tilt) -> bool`
   - `get_chat_message(context) -> Optional[str]`（返回 chat 推送）
   - `update_tilt(hand_result) -> None`（连输/被 Bad Beat 增加 tilt，影响后续决策）

2. **16 型人格子类或配置驱动**（建议先用**配置驱动**，不必写 16 个子类）：
```python
class PersonalityDrivenAI(PokerAI):
    def __init__(self, difficulty, mbti_type):
        super().__init__(difficulty)
        self.persona = MBTI_PERSONALITIES[mbti_type]
        self.aggression = self.persona["aggression"]
        self.bluff_frequency = self.persona["bluff_freq"]
        self.mbti_type = mbti_type
        self.tilt = 0.0
    ...
```

3. **preflop 起手牌强度表**（必做）：用一张 169 组合的起手牌胜率表（或调用 `deuces` / `holdem_calc` 库算 equity）替代当前 preflop 一律 0.3 的错误。
4. **Kelly 计算用真实 equity**：把 Monte-Carlo 模拟（枚举剩余公牌 N 次算胜率）接入 `_calculate_kelly`，取代 `win_rate = hand_strength + 0.1` 的粗糙估计。

### 9.4 游戏引擎增强（配合 MBTI）
- 修复 `current_player_index` 推进 bug（#24），AI 轮询必须遍历所有未行动的 is_ai 玩家。
- 增加"行动历史 per 玩家"（已经有 `game.history`，但需要 per-player 聚合：VPIP/PFR/AF 等 HUD 数据）喂给对手建模。
- 每局结束后向 AI 提供结果（赢/输/手牌），用于更新 `tilt` 和 `train_from_history`。
- 预留 MBTI 对局结果入库：`ai_training_data` 表已有，补 `mbti_type` 字段。

### 9.5 前端接入点（React）
1. **AI 人格标识**：
   - `PokerTable.jsx` 的 player-seat：AI 玩家显示 MBTI 四字母 Tag（不同字母组合配不同颜色）+ 一个拟人化头像（可以用 emoji 或头像库：🧐 INTJ、😈 ENTP、🦁 ESTP、🌸 INFP…）。
   - GameLobby 创建房间时，AI 选择从 "难度" 单选改为 "难度 + MBTI 人格" 两个 Select（MBTI 可留空=随机）。
2. **AI 对话气泡**：WebSocket 收到 `chat` 类型消息时，在玩家座位旁弹出一个小气泡（类似斗地主的语气）。
3. **AI 思维节奏**：根据 persona 的 `thinking_ms` 区间让后端 `asyncio.sleep` 真实等待，前端在等待时显示专属思考动画（如 INTJ 是 🧠 转圈，ENTP 是 💡 闪）。
4. **AI 选单页/AI 图鉴**（后续）：做一个 "AI 对手档案" 页面，展示 16 型的风格描述、胜率、你对 TA 的历史战绩。
5. **彩蛋页联动**：`public/easter-egg.html` 作为品牌化入口，可加入 "随机抽一个 MBTI 对手" 的趣味按钮。

### 9.6 推荐实施顺序
1. **第一阶段（1~2 天）**：修 P0 bug，保证游戏能正常跑一轮；起手牌引入 169 手牌强度表；
2. **第二阶段（3~5 天）**：抽出 `PersonalityDrivenAI`，把 aggression/bluff_freq/thinking_ms/bet_size 4 个维度做成配置驱动，先实现 4 个代表类型（INTJ/ENTP/ISTJ/ESFP）；前端显示 MBTI tag；
3. **第三阶段（1 周）**：补 tilt 模型、对手建模、chat 风格文案，扩到 8~16 型；
4. **第四阶段（可选）**：接入 CFR/蒙特卡洛模拟强化学习（`train_from_history` 实现），训练每个 MBTI 的差异化策略。

---

## 十、结论与优先级建议

**一句话结论**：项目完成度约 **40%** —— 后端骨架、Docker/CI 配置、前端视觉框架都搭得相当专业（多阶段 Dockerfile、Compose profile、Zustand + Framer Motion），但**核心游戏规则引擎、密码安全、WebSocket 多人广播、current_player 轮次、前后端 playerId 对齐**这几处致命 bug 导致当前部署后"能跑但玩不起来"；凯利/战绩/大厅三大卖点均是 mock 数据；AI 仅基于粗糙手力+随机，**完全无 MBTI 概念**。

**上线前最小修复清单（MVP Go/No-Go）**：
1. `auth/auth.py` 末行 `No` → `None`
2. `game_engine.py` 加注金额、轮次结束判定、current_player_index 推进、preflop 顺序、blinds 旋转
3. `app.py` WebSocket 连接改为 List，CORS 去掉 `"*"+credentials`，lifespan 日志区分 DB 连接成功/降级
4. 前端 `playerId` 对齐（不能再用 `user?.id || 'player1'`，要从 createGame/joinGame 的返回拿 `player_id` 存到 store）
5. Dashboard/凯利指数接入真实 pot/hand_strength 计算（彩蛋页已有视觉参考，直接抽算法）
6. 后端启动端口读 `$PORT`（Railway 兼容），生产 workers=1
7. 部署时在前端注入 `VITE_API_URL` / `VITE_WS_URL`，后端 CORS 加入 Vercel/Railway 域名

完成上述 7 项后即可在 Vercel + Railway 上线一个 1v1 可用的版本，然后再按第九章路线做 MBTI 差异化 AI 升级。
